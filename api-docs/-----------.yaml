```yml
openapi: 3.0.0
info:
  title: سرویس کد کشاورزی
  description: API برای مدیریت کد کشاورزی
  version: 1.0.0
servers:
  - url: https://kalatakco.com/
    description: سرور اصلی
paths:
  /kod:
    get:
      summary: دریافت لیست کد کشاورزی
      description: دریافت لیست تمام کد کشاورزی
      operationId: getKodList
      tags:
        - کد کشاورزی
      responses:
        '200':
          description: لیست کد کشاورزی
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Kod'
        '500':
          description: خطای سرور
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      summary: ایجاد کد کشاورزی جدید
      description: ایجاد یک کد کشاورزی جدید
      operationId: createKod
      tags:
        - کد کشاورزی
      requestBody:
        description: اطلاعات کد کشاورزی جدید
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Kod'
      responses:
        '201':
          description: کد کشاورزی ایجاد شده
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Kod'
        '400':
          description: درخواست نامعتبر
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /kod/{id}:
    get:
      summary: دریافت اطلاعات کد کشاورزی
      description: دریافت اطلاعات یک کد کشاورزی
      operationId: getKod
      tags:
        - کد کشاورزی
      parameters:
        - in: path
          name: id
          schema:
            type: integer
          required: true
          description: شناسه کد کشاورزی
      responses:
        '200':
          description: اطلاعات کد کشاورزی
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Kod'
        '404':
          description: کد کشاورزی پیدا نشد
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    put:
      summary: ویرایش کد کشاورزی
      description: ویرایش یک کد کشاورزی
      operationId: updateKod
      tags:
        - کد کشاورزی
      parameters:
        - in: path
          name: id
          schema:
            type: integer
          required: true
          description: شناسه کد کشاورزی
      requestBody:
        description: اطلاعات ویرایش شده کد کشاورزی
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Kod'
      responses:
        '200':
          description: کد کشاورزی ویرایش شده
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Kod'
        '400':
          description: درخواست نامعتبر
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    delete:
      summary: حذف کد کشاورزی
      description: حذف یک کد کشاورزی
      operationId: deleteKod
      tags:
        - کد کشاورزی
      parameters:
        - in: path
          name: id
          schema:
            type: integer
          required: true
          description: شناسه کد کشاورزی
      responses:
        '204':
          description: کد کشاورزی حذف شده
        '404':
          description: کد کشاورزی پیدا نشد
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
  /kod/search:
    get:
      summary: جستجوی کد کشاورزی
      description: جستجوی کد کشاورزی بر اساس نام یا شناسه
      operationId: searchKod
      tags:
        - کد کشاورزی
      parameters:
        - in: query
          name: query
          schema:
            type: string
          required: true
          description: نام یا شناسه کد کشاورزی
      responses:
        '200':
          description: نتایج جستجو
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Kod'
        '400':
          description: درخواست نامعتبر
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    Kod:
      type: object
      properties:
        id:
          type: integer
          description: شناسه کد کشاورزی
        name:
          type: string
          description: نام کد کشاورزی
        code:
          type: string
          description: کد کشاورزی
      required:
        - id
        - name
        - code
    Error:
      type: object
      properties:
        message:
          type: string
          description: پیام خطا
      required:
        - message
```