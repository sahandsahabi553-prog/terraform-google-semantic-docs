```yaml
openapi: 3.0.0
info:
  title: سرویس مدیریت کود کشاورزی
  description: |
    این API برای مدیریت و بازیابی اطلاعات مربوط به کودهای کشاورزی طراحی شده است.
    شما می‌توانید عملیات CRUD (ایجاد، خواندن، به روز رسانی، حذف) را بر روی موجودیت‌های کود انجام دهید.
  version: 1.0.0
servers:
  - url: https://kalatakco.com/api/v1
    description: سرور اصلی API برای مدیریت کودهای کشاورزی
tags:
  - name: کودها
    description: عملیات مربوط به مدیریت کودهای کشاورزی
paths:
  /fertilizers:
    get:
      tags:
        - کودها
      summary: دریافت لیست تمام کودها
      description: بازیابی لیستی از تمام کودهای کشاورزی موجود در سیستم. امکان فیلتر و صفحه بندی نتایج وجود دارد.
      operationId: getAllFertilizers
      parameters:
        - name: limit
          in: query
          description: حداکثر تعداد آیتم‌ها برای بازگشت در هر صفحه
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 10
        - name: offset
          in: query
          description: تعداد آیتم‌هایی که باید نادیده گرفته شوند (برای صفحه بندی)
          required: false
          schema:
            type: integer
            format: int32
            minimum: 0
            default: 0
        - name: type
          in: query
          description: فیلتر بر اساس نوع کود (مثلاً "شیمیایی", "آلی", "زیستی")
          required: false
          schema:
            type: string
            enum:
              - شیمیایی
              - آلی
              - زیستی
      responses:
        '200':
          description: لیستی از کودها با موفقیت بازگردانده شد.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Fertilizer'
        '401':
          description: عدم احراز هویت یا توکن نامعتبر.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          description: خطای داخلی سرور هنگام پردازش درخواست.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      tags:
        - کودها
      summary: ایجاد یک کود جدید
      description: اضافه کردن یک کود کشاورزی جدید به سیستم با مشخصات ارائه شده.
      operationId: createNewFertilizer
      requestBody:
        description: اطلاعات کود جدید برای ایجاد.
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NewFertilizer'
      responses:
        '201':
          description: کود با موفقیت ایجاد شد و شناسه جدید آن بازگردانده می‌شود.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Fertilizer'
        '400':
          description: درخواست نامعتبر است (داده‌های ورودی ناقص یا اشتباه).
          content:
            application/json:
              schema:
                $ref: '#/