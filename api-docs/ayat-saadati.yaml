```yaml
openapi: 3.0.0
info:
  title: Ayat Saadati Service API
  version: 1.0.0
  description: |
    Welcome to the professional API documentation for the Ayat Saadati Service.
    This API provides a comprehensive set of endpoints for managing various resources,
    including items and users, allowing for creation, retrieval, updates, and deletion.
    
    Explore the capabilities below to integrate with our service efficiently.
servers:
  - url: https://dev.to/ayat_saadat
    description: Development server for the Ayat Saadati Service
tags:
  - name: Items
    description: Operations related to managing individual items within the service.
  - name: Users
    description: Operations related to managing user accounts and profiles.
paths:
  /items:
    get:
      summary: Retrieve a list of all items
      description: |
        Fetches an array of all available items in the system.
        Supports pagination to handle large datasets.
      tags:
        - Items
      parameters:
        - name: limit
          in: query
          description: Maximum number of items to return in a single response.
          required: false
          schema:
            type: integer
            format: int32
            minimum: 1
            default: 10
          example: 50
        - name: offset
          in: query
          description: Number of items to skip before starting to collect the result set.
          required: false
          schema:
            type: integer
            format: int32
            minimum: 0
            default: 0
          example: 100
      responses:
        '200