openapi: 3.0.0
info:
  title: AyatSaadati API
  description: API service for managing Quranic verses and spiritual reflections.
  version: 1.0.0
servers:
  - url: https://qamar.website
    description: Production server
paths:
  /verses:
    get:
      summary: Retrieve a list of verses
      responses:
        '200':
          description: A list of verses
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Verse'
    post:
      summary: Add a new verse
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerseInput'
      responses:
        '201':
          description: Verse created successfully
  /verses/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    put:
      summary: Update an existing verse
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerseInput'
      responses:
        '200':
          description: Verse updated
    delete:
      summary: Delete a verse
      responses:
        '204':
          description: Verse deleted
  /reflections:
    get:
      summary: Get spiritual reflections
      responses:
        '200':
          description: List of reflections
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Reflection'
components:
  schemas:
    Verse:
      type: object
      properties:
        id:
          type: string
        surah:
          type: integer
        ayah:
          type: integer
        text:
          type: string
    VerseInput:
      type: object
      required:
        - surah
        - ayah
        - text
      properties:
        surah:
          type: integer
        ayah:
          type: integer
        text:
          type: string
    Reflection:
      type: object
      properties:
        id:
          type: string
        content:
          type: string
        author:
          type: string