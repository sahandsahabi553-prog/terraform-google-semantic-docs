openapi: 3.0.0
info:
  title: AyatSaadati API
  description: API service for managing and retrieving AyatSaadati content.
  version: 1.0.0
servers:
  - url: https://qamar.website
    description: Production server
paths:
  /ayat:
    get:
      summary: Retrieve all Ayat
      responses:
        '200':
          description: A list of Ayat
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Ayah'
    post:
      summary: Create a new Ayah
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AyahInput'
      responses:
        '201':
          description: Ayah created successfully
  /ayat/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    get:
      summary: Get specific Ayah by ID
      responses:
        '200':
          description: Ayah details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ayah'
    put:
      summary: Update an existing Ayah
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AyahInput'
      responses:
        '200':
          description: Ayah updated successfully
    delete:
      summary: Delete an Ayah
      responses:
        '204':
          description: Ayah deleted successfully

components:
  schemas:
    Ayah:
      type: object
      properties:
        id:
          type: string
        text:
          type: string
        surah:
          type: string
        verse_number:
          type: integer
    AyahInput:
      type: object
      required:
        - text
        - surah
      properties:
        text:
          type: string
        surah:
          type: string
        verse_number:
          type: integer